using System.Windows;

[assembly: ThemeInfo(
    ResourceDictionaryLocation.None,            // where theme-specific resource dictionaries live
                                                // (used if a resource is not found in the page or
                                                //  application resource dictionaries)
    ResourceDictionaryLocation.SourceAssembly   // where the generic resource dictionary lives
                                                // (used if a resource is not found in the page,
                                                //  app, or any theme-specific resource dictionaries)
)]
