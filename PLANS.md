# TMM — Active Plans

> Living plan doc. Each section is a self-contained brief a cold agent can execute.
> **Completed work has been archived to [CHANGELOG.md](CHANGELOG.md).** This file holds
> only forward-looking work.
>
> **Standing rule:** Any feature that works for built-in games must be fully configurable
> by a user adding a custom game through the wizard UI — it is not complete until it appears
> in `CustomGameSetupWizard` Step 1 (input) and Step 4 (review). The `.tmmgame` JSON is only
> a shortcut for built-in profiles; users never edit JSON.
>
> **Architectural principles** (see [CLAUDE.md](CLAUDE.md)): (1) deployment plans freeze at
> install; (2) rollback restores to the first-touch baseline.

_No active plans queued. Add briefs below._
