<UserControl x:Class="TMM.Mockups.SettingsDualPane"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:TMM.Mockups">

    <UserControl.Resources>
        <!-- Category button style -->
        <Style x:Key="CategoryBtnStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource SubTextBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,3,0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#1AFFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Active category style -->
        <Style x:Key="CategoryBtnActiveStyle" BasedOn="{StaticResource CategoryBtnStyle}" TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Background" Value="#0AFFFFFF"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#2D2D30">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left: Category list -->
        <Border Grid.Column="0" Background="#3E3E42" BorderBrush="#555555" BorderThickness="0,0,1,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <!-- Unsaved changes banner (optional, can go here) -->

                    <Button Content="General" Style="{StaticResource CategoryBtnActiveStyle}"/>
                    <Button Content="Appearance" Style="{StaticResource CategoryBtnStyle}"/>
                    <Button Content="File Paths" Style="{StaticResource CategoryBtnStyle}"/>
                    <Button Content="Advanced" Style="{StaticResource CategoryBtnStyle}"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Right: Category detail pane -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Unsaved changes banner (at top) -->
            <Border Grid.Row="0" Background="#0883FF" Padding="16,8" Visibility="Collapsed">
                <Grid>
                    <TextBlock Text="Unsaved changes" Foreground="White" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Save" Height="28" Padding="14,0" Margin="0,0,8,0"
                                Background="White" Foreground="#0883FF"
                                BorderThickness="0" FontSize="12" FontWeight="Bold" Cursor="Hand"/>
                        <Button Content="Discard" Height="28" Padding="14,0"
                                Background="Transparent" Foreground="White"
                                BorderThickness="1" BorderBrush="White"
                                FontSize="12" Cursor="Hand"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Content scroll area -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="32,24,32,24" MaxWidth="600">

                    <!-- General category -->
                    <TextBlock Text="General" FontSize="18" FontWeight="Bold" Margin="0,0,0,16"/>

                    <!-- Startup preference -->
                    <Border BorderBrush="#555555" BorderThickness="0,1,0,0" Padding="0,12,0,0" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="Startup" FontSize="12" FontWeight="SemiBold" Foreground="#88EE88" Margin="0,0,0,8"/>
                            <TextBlock Text="Open TMM to:" FontSize="11" Foreground="#888888" Margin="0,0,0,6"/>
                            <StackPanel Orientation="Horizontal">
                                <RadioButton Content="Library" Margin="0,0,16,0" Foreground="#CCCCCC" Cursor="Hand"/>
                                <RadioButton Content="Mod Manager (default game)" Foreground="#CCCCCC" Cursor="Hand" IsChecked="True"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Notifications -->
                    <Border BorderBrush="#555555" BorderThickness="0,1,0,0" Padding="0,12,0,0" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="Notifications" FontSize="12" FontWeight="SemiBold" Foreground="#88EE88" Margin="0,0,0,8"/>
                            <CheckBox Foreground="#CCCCCC" Cursor="Hand">
                                <StackPanel>
                                    <TextBlock Text="Verbose deployment notifications" Foreground="#CCCCCC" FontSize="11"/>
                                    <TextBlock Text="Show detailed info during mod deployment" Foreground="#888888" FontSize="10" Margin="0,2,0,0"/>
                                </StackPanel>
                            </CheckBox>
                        </StackPanel>
                    </Border>

                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
