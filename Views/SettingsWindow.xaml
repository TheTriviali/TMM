<local:TmmWindow x:Class="TMM.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:TMM"
        Title="Settings" Height="610" Width="540"
        WindowStartupLocation="CenterOwner" Background="{DynamicResource BgBrush}"
        WindowStyle="None" AllowsTransparency="True">

    <Border BorderBrush="{DynamicResource WindowBorderBrush}" BorderThickness="2"
            CornerRadius="12" Background="{DynamicResource BgBrush}" ClipToBounds="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="34"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title bar -->
            <Grid Grid.Row="0" Background="#1A000000" MouseLeftButtonDown="TitleBar_MouseDown">
                <TextBlock Text="Settings" Foreground="{DynamicResource SubTextBrush}"
                           VerticalAlignment="Center" Margin="15,0,0,0" FontSize="12" FontWeight="SemiBold"/>
                <Button HorizontalAlignment="Right" Width="45" VerticalAlignment="Stretch" Click="BtnClose_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="closeBg" Background="Transparent" CornerRadius="0,10,0,0">
                                <TextBlock Text="x" Foreground="{DynamicResource SubTextBrush}"
                                           HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="12"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="closeBg" Property="Background" Value="#E81123"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>

            <!-- Content -->
            <StackPanel Grid.Row="1" Margin="18,14,18,0">

                <!-- GTA III Series paths -->
                <StackPanel x:Name="pnlGtaIII">
                    <TextBlock Text="GTA III Series" FontSize="13" FontWeight="SemiBold"
                               Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,8"/>
                    <local:GameSetupRow x:Name="rowIII"/>
                    <local:GameSetupRow x:Name="rowVC"/>
                    <local:GameSetupRow x:Name="rowSA"/>
                    <Border Height="1" Background="{DynamicResource SubTextBrush}" Opacity="0.15" Margin="0,8,0,10"/>
                </StackPanel>

                <!-- GTA IV paths -->
                <StackPanel x:Name="pnlGtaIV">
                    <TextBlock Text="GTA IV — Complete Edition" FontSize="13" FontWeight="SemiBold"
                               Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,8"/>
                    <local:GameSetupRow x:Name="rowIV"/>
                    <local:GameSetupRow x:Name="rowTLaD"/>
                    <local:GameSetupRow x:Name="rowTBoGT"/>
                    <Button Content="Run Initial Setup Wizard" Click="BtnRerunSetup_Click"
                            Height="28" Background="{DynamicResource PanelBrush}"
                            Foreground="{DynamicResource AccentBrush}"
                            BorderThickness="0" Cursor="Hand" Margin="0,8,0,10" FontSize="12"/>
                </StackPanel>

                <!-- Steam Controls (GTA only) -->
                <Border x:Name="pnlSteamControls"
                        BorderBrush="{DynamicResource SubTextBrush}" BorderThickness="0,1,0,0"
                        Padding="0,10,0,0" Margin="0,0,0,10">
                    <StackPanel>
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Steam Controls" FontSize="13" FontWeight="SemiBold"
                                       Foreground="#FF88EE" VerticalAlignment="Center"/>
                            <ComboBox x:Name="cmbSteamGame" Grid.Column="1" Width="150" SelectedIndex="0"
                                      Background="{DynamicResource ControlBgBrush}"
                                      Foreground="{DynamicResource TextBrush}"
                                      BorderThickness="0" Padding="6,3">
                                <ComboBoxItem x:Name="cmbItemIII"  Content="GTA III"         Tag="III"/>
                                <ComboBoxItem x:Name="cmbItemVC"   Content="GTA Vice City"   Tag="VC"/>
                                <ComboBoxItem x:Name="cmbItemSA"   Content="GTA San Andreas"  Tag="SA"/>
                                <ComboBoxItem x:Name="cmbItemIV"   Content="GTA IV"           Tag="IV"/>
                            </ComboBox>
                        </Grid>
                        <UniformGrid Columns="3">
                            <Button Content="[OK] Verify" Click="BtnSteamAction_Click" Tag="validate"
                                    Height="26" Margin="0,0,3,0" Background="{DynamicResource PanelBrush}"
                                    Foreground="{DynamicResource TextBrush}" BorderThickness="0" Cursor="Hand"
                                    ToolTip="Force Steam to verify file integrity"/>
                            <Button Content="Install" Click="BtnSteamAction_Click" Tag="install"
                                    Height="26" Margin="3,0,3,0" Background="{DynamicResource PanelBrush}"
                                    Foreground="{DynamicResource TextBrush}" BorderThickness="0" Cursor="Hand"/>
                            <Button Content="Uninstall" Click="BtnSteamAction_Click" Tag="uninstall"
                                    Height="26" Margin="3,0,0,0" Background="#442222"
                                    Foreground="#FF5555" BorderThickness="0" Cursor="Hand"/>
                        </UniformGrid>
                    </StackPanel>
                </Border>

                <!-- Diagnostics -->
                <Border BorderBrush="{DynamicResource SubTextBrush}" BorderThickness="0,1,0,0"
                        Padding="0,10,0,0" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="Diagnostics" FontSize="13" FontWeight="SemiBold"
                                   Foreground="#FF88EE" Margin="0,0,0,8"/>
                        <UniformGrid x:Name="gridDiagnostics" Columns="4">
                            <Button x:Name="btnMd5" Content="MD5" Click="BtnMd5Check_Click"
                                    Height="26" Margin="0,0,3,0" Background="{DynamicResource PanelBrush}"
                                    Foreground="{DynamicResource AccentBrush}" BorderThickness="0" Cursor="Hand"
                                    ToolTip="Check exe MD5 - modlist exe mod overrides vanilla if present"/>
                            <Button Content="Error Log" Click="BtnOpenLog_Click"
                                    Height="26" Margin="3,0,3,0" Background="{DynamicResource PanelBrush}"
                                    Foreground="{DynamicResource TextBrush}" BorderThickness="0" Cursor="Hand"/>
                            <Button Content="Wipe Cache" Click="BtnWipeCache_Click"
                                    Height="26" Margin="3,0,3,0" Background="{DynamicResource PanelBrush}"
                                    Foreground="{DynamicResource TextBrush}" BorderThickness="0" Cursor="Hand"/>
                            <Button Content="Dev Console" Click="BtnOpenConsole_Click"
                                    Height="26" Margin="3,0,0,0" Background="{DynamicResource PanelBrush}"
                                    Foreground="{DynamicResource AccentBrush}" BorderThickness="0" Cursor="Hand"
                                    ToolTip="Open Developer Diagnostics Console"/>
                        </UniformGrid>
                    </StackPanel>
                </Border>

                <!-- Danger Zone -->
                <Border BorderBrush="#FF5555" BorderThickness="0,1,0,0" Padding="0,10,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Danger Zone" FontSize="12" FontWeight="SemiBold"
                                   Foreground="#FF5555" VerticalAlignment="Center"/>
                        <TextBlock x:Name="lblVersion" Grid.Column="1"
                                   Foreground="{DynamicResource SubTextBrush}"
                                   HorizontalAlignment="Center" FontSize="10"
                                   VerticalAlignment="Center" Margin="0,0,12,0"
                                   Text="v1.2 Alpha"/>
                        <Button Grid.Column="2" Content="Factory Reset" Click="BtnFactoryReset_Click"
                                Height="28" Padding="16,0" Background="#442222" Foreground="#FF5555"
                                BorderThickness="0" Cursor="Hand"/>
                    </Grid>
                </Border>
            </StackPanel>

            <!-- Bottom close -->
            <Border Grid.Row="2" Padding="18,10" Background="#0A000000">
                <Button Content="Close" Click="BtnClose_Click" Height="30" FontSize="13"
                        Background="{DynamicResource AccentBrush}" Foreground="Black"
                        FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
            </Border>
        </Grid>
    </Border>
</local:TmmWindow>
